# censboot_summary errors without model = TRUE

    Code
      censboot_summary(cox_no_model, R = 99)
    Condition
      Error in `censboot_summary()`:
      ! The model must be fitted using model=TRUE (see ?censboot_summary for examples).

