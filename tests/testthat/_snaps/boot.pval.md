# boot.pval errors on invalid alternative

    Code
      boot.pval(city_boot, alternative = "invalid")
    Condition
      Error in `boot.pval()`:
      ! alternative must be either "two.sided", "less" or "greater"

